import { UppercaseText } from './uppercase-text';

describe('UppercaseText', () => {
  it('should create an instance', () => {
    const directive = new UppercaseText();
    expect(directive).toBeTruthy();
  });
});
